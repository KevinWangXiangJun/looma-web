import { common } from './common';
import { login } from './login';
import { nav } from './nav';
import { dashboard } from './dashboard';

export const en = {
  common,
  login,
  nav,
  dashboard,
};
