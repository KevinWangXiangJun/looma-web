import { common } from './common';
import { login } from './login';
import { nav } from './nav';
import { dashboard } from './dashboard';

export const zh = {
  common,
  login,
  nav,
  dashboard,
};
