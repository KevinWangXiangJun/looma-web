import '@testing-library/jest-dom';
import { beforeEach, afterEach } from 'vitest';

/**
 * Vitest 测试环境配置
 * 为所有测试文件提供全局 setup
 */

// 设置全局测试配置
beforeEach(() => {
  // 在每个测试前执行
});

afterEach(() => {
  // 在每个测试后执行
});
